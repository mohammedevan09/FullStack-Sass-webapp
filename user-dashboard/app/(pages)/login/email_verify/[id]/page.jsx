import MainEmailTokenVerify from './MainEmailTokenVerify'

const page = ({ params, searchParams }) => {
  return <MainEmailTokenVerify params={params} searchParams={searchParams} />
}

export default page
